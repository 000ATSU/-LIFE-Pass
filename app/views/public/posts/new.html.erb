<div class=".container">
  <div class="row mt-3 justify-content-center">
  <%= form_with model: @post, url: posts_path do |f| %>
    <div class="mt-5">
      <%= f.text_field :post_title, required: true, placeholder: "タイトル" %>
    </div>
    <div class="mt-5">
      <%= f.text_area :post_text, required: true, :rows => 10, style: 'width:100%;', placeholder: "投稿文  ※250文字まで" %>
      <p class="lead mt-3">
        ※投稿した後に削除、編集はできません。投稿内容をお確かめの上
      </p>
      <p class="lead">
        投稿を行ってください。
      </p>
    </div>
    <div>
      <h4>カテゴリ選択</h4>
    </div>
    <div class="mt-3">
      <%= f.collection_select :category_id, Category.all, :id, :category_name, {include_blank: "選択して下さい"}, {class: "select_size"} %>
    </div>
    <div class="mt-5">
      <%= f.hidden_field :user_id, :value => current_user.id %>
      <%= f.submit '投稿する', class: "btn btn-success btn-lg" %>
    </div>
  <% end %>
  </div>
</div>
